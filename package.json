{
  "name": "trading-portfolio-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/TradingPortfolioTracker.jsx --bundle --minify --format=iife --loader:.jsx=jsx --outfile=public/app.js",
    "dev": "esbuild src/TradingPortfolioTracker.jsx --bundle --format=iife --loader:.jsx=jsx --outfile=public/app.js --watch --servedir=public"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
